# Configuration file

enchantments {
    # Crash if conflicts were detected [default: true]
    B:"crash if conflict"=false

    # Ids to ignore conflicts for when generating crash report.
    I:"crash blacklisted ids" <
     >

    # Ids not to override if override ids is enabled.
    I:"override ids excluded ids" <
     >

    # Classes not to override if override ids is enabled. [default: ]
    S:"override ids excluded classes" <
     >

    # If enabled, ids will be supplied by AIC instead of using original id. [default: false]
    B:"override ids"=false
}


potions {
    # Crash if conflicts were detected [default: true]
    B:"crash if conflict"=false

    # Ids to ignore conflicts for when generating crash report.
    I:"crash blacklisted ids" <
     >

    # Ids not to override if override ids is enabled.
    I:"override ids excluded ids" <
     >

    # Classes not to override if override ids is enabled. [default: ]
    S:"override ids excluded classes" <
     >

    # If enabled, ids will be supplied by AIC instead of using original id. [default: false]
    B:"override ids"=false
}


biomes {
    # Crash if conflicts were detected. [default: true]
    B:"crash if conflict"=false

    # Ids to ignore conflicts for when generating crash report.
    I:"crash blacklisted ids" <
        9
     >

    # Ids not to override if override is enabled.
    I:"override ids excluded ids" <
        9
     >

    # Classes not to override if override ids is enabled. [default: ]
    S:"override ids excluded classes" <
     >

    # If enabled, ids will be supplied by AIC instead of using their original id. [default: false]
    B:"override ids"=false
}


logging {
    # Log data in init phase. [default: true]
    B:"log in init"=true

    # Log data in pre init phase. [default: false]
    B:"log in pre init"=true
}


